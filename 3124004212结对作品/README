# 小学四则运算题目生成与批改程序

本项目对应软件工程作业要求，使用 Python 标准库实现：

- `-n` 控制生成题目数量；
- `-r` 控制自然数、分子和分母的范围上限（不包含 `r`，例如 `-r 10` 使用 0～9）；
- 只生成结果非负、除法结果为真分数、运算符不超过 3 个的题目；
- 按交换律、结合律去重，避免同一次运行中出现等价题目；
- 同时生成 `Exercises.txt` 和 `Answers.txt`；
- 使用 `-e`、`-a` 批改题目和答案，并生成 `Grade.txt`。

## 运行环境

程序已调整为兼容 Python 3.6 的写法，也可以在更高版本的 Python 3 上运行。
程序只依赖标准库，不需要安装第三方包。

在 Visual Studio Community 中新建/打开 Python 项目，把 `arithmetic_app.py` 设为启动文件即可。

## 生成题目

在程序所在目录执行：

```text
python arithmetic_app.py -n 10 -r 10
```

也可以用 `--seed` 固定随机种子，方便测试和性能实验：

```text
python arithmetic_app.py -n 1000 -r 100 --seed 2026
```

成功后当前目录会生成：

```text
Exercises.txt
Answers.txt
```

题目示例：

```text
1. 3/5 + 1/2 =
2. (7 - 2) ÷ 9 =
```

答案中的带分数使用作业规定的格式，例如 `2'3/8`。

## 批改答案

准备好题目文件和学生答案文件后执行：

```text
python arithmetic_app.py -e Exercises.txt -a StudentAnswers.txt
```

程序会在当前目录生成 `Grade.txt`：

```text
Correct: 5 (1, 3, 5, 7, 9)
Wrong: 5 (2, 4, 6, 8, 10)
```

答案文件可以带编号，也可以只写答案；程序会忽略每行开头的题号。

## Visual Studio Profiler 建议

性能测试可以在 Visual Studio Community 中运行以下两组规模：

```text
python arithmetic_app.py -n 1000 -r 100 --seed 1
python arithmetic_app.py -n 10000 -r 1000 --seed 1
```

在 Performance Profiler 中记录 CPU 使用率、运行时间和内存分配，并重点观察：

- `ExerciseGenerator.generate`：去重和题目生成；
- `evaluate`：表达式求值；
- `grade_files`：批量读取、解析和统计。

性能测试不改变程序逻辑，博客中可根据 Profiler 截图填写实际耗时和优化过程。

